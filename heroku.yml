build:
  docker:
    api: backend/Dockerfile
    client: frontend/Dockerfile
run:
  web:
    image: registry.heroku.com/blue-tread-weather/api
    command:
      - dotnet API.dll
  worker:
    image: registry.heroku.com/blue-tread-weather/client
    command:
      - nginx -g "daemon off;"
